import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// , mount up npw up later maybe
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
